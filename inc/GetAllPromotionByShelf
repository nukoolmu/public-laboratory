GetAllPromotionByShelf(){
    SRC_FILE=${1}

    if [ -f ${TMP_PATH}/${SRC_FILE} ];then

    jq '[.data[] | {id: .id, title: .title, titleEn: .titleEn, icon: .icon, sanitizedName: .sanitizedName,publish: .publish, priority: .priority, lastUpdated: .lastUpdated, tags: .tags, type: .type, conditionCode: .conditionCode, subShelves: .subShelves, items: .items}] | sort_by(.id)' ${TMP_PATH}/${SRC_FILE} 
    
    else
        echo "no src file exist"
    fi
}