name: example-flow
run-name: ${{ github.actor }} is learning GitHub Actions
on:
  workflow_dispatch:
    inputs:
      tag:
        description: 'input workflow run number'
        required: true
  push: 
   branches: [sample-app-0]

jobs:

  build:
    runs-on: ubuntu-latest
    steps:
      - name: check server-info
        run: lsb_release -a

      - name: git checkout
        run: git clone -b sample-app-0 https://github.com/nukoolmu/public-laboratory.git

      - name: docker login
        run: docker login -u nukoolm -p ${{ Secrets.DOCKER_REGISTRY }}

      - name: wait for Approve    
        uses: trstringer/manual-approval@v1
        id: check-approval
        with:
          secret: ${{ github.TOKEN }}
          approvers: "nukoolmu, samah603"
          minimum-approvals: 1

#      - name: Trigger workflow
#        if: steps.check-approval.outputs.approved == 'true'
#        uses: actions/github-script@v5
#        with:
#          script: |
#            const payload = JSON.stringify({
#              ref: context.ref,
#              inputs: {
#                my_input: 'my_value'
#              }
#            });
#            const response = await octokit

      - name: build and push
        run: |
          cd public-laboratory 

          docker build -f Dockerfile-appB . -t nukoolm/sample-app:github-1.0
          docker push nukoolm/sample-app:github-1.0

  deploy:
    if: workflow_dispatch.inputs.tag == "*"
    runs-on: ubuntu-latest
    steps:
      - name: mockup deployed
        run: echo Deploying