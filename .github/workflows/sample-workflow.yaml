name: example-flow
run-name: ${{ github.actor }} is learning GitHub Actions
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: check server-info
        run: lsb_release -a
      - name: docker login
        run: docker login -u nukoolm -p ${{ Secerts.DOCKER_REGISTRY }}